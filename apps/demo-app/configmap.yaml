apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-app-config
  namespace: gitops-demo
  labels:
    app: demo-app
data:
  APP_ENV: "lab"
  APP_GREETING: "deployed by Argo CD from git - not by kubectl apply"
  RELEASE: "v1"
